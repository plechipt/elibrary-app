import React from "react";
import Pagination from "@material-ui/lab/Pagination";
import "./Other.css";

const CustomPagination = () => {
  return (
    <Pagination className="pagination" count={1} size="large" color="primary" />
  );
};

export default CustomPagination;
